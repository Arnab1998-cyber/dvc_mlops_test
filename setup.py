from setuptools import setup, find_packages

setup(name='src',
      version='0.0.1',
      description='this is a wine q package',
      author='arnab',
      packages=find_packages(),
      license='MIT')